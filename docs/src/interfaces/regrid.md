# Regridding Interface

ConservativeRegridding returns a Regridder object.  We need to introduce an interface to allow users to set the values they want from the work vector embedded within the regridder.  And a similar interface to get the values they want from the source field.