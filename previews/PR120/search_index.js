var documenterSearchIndex = {"docs":
[{"category":"section","location":"interfaces/regrid/#Regridding-Interface","page":"Regridding Interface","text":"ConservativeRegridding returns a Regridder object.  We need to introduce an interface to allow users to set the values they want from the work vector embedded within the regridder.  And a similar interface to get the values they want from the source field.","title":"Regridding Interface"},{"category":"section","location":"extensions/climacore/#ClimaCore-Spectral-Element-Extension","page":"ClimaCore Spectral Element Extension","text":"ConservativeRegridding.jl provides conservative regridding between finite-volume (FV) grids and ClimaCore.jl spectral-element (SE) spaces. The extension is loaded automatically when ClimaCore is imported alongside ConservativeRegridding.","title":"ClimaCore Spectral Element Extension"},{"category":"section","location":"extensions/climacore/#Spectral-element-and-finite-volume-integrals","page":"ClimaCore Spectral Element Extension","text":"A finite-volume grid carries one mean value bar f_i per cell, so the integral of a field over cell i is\n\nint_i f  mathrmdA approx bar f_i  A_i \n\nwhere A_i is the cell area. A spectral-element space carries N_q^2 nodal values f^e_ij per element e (polynomial degree N_q - 1), expanded in a tensor product of one-dimensional Lagrange basis polynomials,\n\nf^e(xi eta) = sum_ij f^e_ij  phi_i(xi)  phi_j(eta) \n\nover the reference square (xi eta) in -1 1^2. The integral over an element is evaluated by Gauss–Legendre–Lobatto (GLL) quadrature,\n\nint_e f  mathrmdA approx sum_ij W^e_ij  f^e_ij \nqquad\nW^e_ij = w_i  w_j  J^e_ij \n\nwhere w_i are the quadrature weights, J^e_ij the Jacobian of the reference-to-physical map at node (ij), and W^e_ij the weighted Jacobian stored by ClimaCore.Spaces.weighted_jacobian.","title":"Spectral-element and finite-volume integrals"},{"category":"section","location":"extensions/climacore/#Implemented-regridding-directions","page":"ClimaCore Spectral Element Extension","text":"Three directions are conceivable; the extension implements the two that involve a finite volume side:\n\nDirection Method\nSE to FV area-weighted average of the SE field over each cell (see SE → FV below)\nFV to SE per-element L^2 projection onto the SE basis (see FV → SE below)\nSE to SE not supported — compose SE to FV to SE\n\nBoth implemented directions are built from the same family of weights B(k(eij)), defined and evaluated below.","title":"Implemented regridding directions"},{"category":"section","location":"extensions/climacore/#The-intersection-weights-B","page":"ClimaCore Spectral Element Extension","text":"For a finite-volume cell k and a spectral element e, define\n\nB(k (eij)) = int_k cap e phi_i(xi)  phi_j(eta)  J^e(xi eta) \n                mathrmdxi  mathrmdeta \n\nthe integral of the SE basis function (ij) over the intersection of cell k with element e. Because the reference-to-physical map has Jacobian determinant J^e, the physical area element is mathrmdA = J^e  mathrmdxi  mathrmdeta, and the weight is equivalently a physical-space integral with no explicit Jacobian,\n\nB(k (eij)) = int_k cap e phi_ibig(xi(x)big)  phi_jbig(eta(x)big) \n                mathrmdA \n\nThe extension evaluates this physical-space form directly (accumulate_principled_b): the intersection polygon k cap e — already computed by ConservativeRegridding.jl via GeometryOps.jl — is fan-triangulated from its centroid, a barycentric Gauss rule is applied on each triangle, and the basis functions are evaluated at each quadrature point after inverting the element map to recover (xi eta). The physical triangle areas carry the Jacobian, so J^e does not appear explicitly. This integration is exact to the quadrature order and is therefore high-order accurate in the element size.\n\nTwo identities follow from the definition and are used below. Since the cells k tile the domain,\n\nsum_k B(k (eij)) = W^e_ij \n\nand since the elements e tile the domain (and sum_ij phi_i phi_j = 1),\n\nsum_eij B(k (eij)) = A_k ","title":"The intersection weights B"},{"category":"section","location":"extensions/climacore/#SE-FV","page":"ClimaCore Spectral Element Extension","text":"The destination cell value is the area-weighted integral of the source SE field over the cell,\n\nf^mathrmdst_k = frac1A_k sum_eij B(k(eij))  f^mathrmsrce_ij \n\nThis is assembled as a sparse matrix with entries B(k(eij)) (rows indexed by FV cell, columns by SE node), and regrid! performs the matrix–vector product followed by division by the destination areas. Conservation is exact in the continuous weights: summing over k and using sum_k B = W^e_ij recovers sum_eij W^e_ij f^mathrmsrce_ij, the SE quadrature integral of the source.","title":"SE → FV"},{"category":"section","location":"extensions/climacore/#FV-SE","page":"ClimaCore Spectral Element Extension","text":"The destination SE field is defined by the weak condition that it agree with the source in every test direction of the SE space: find f^mathrmdst such that\n\nint f^mathrmdst  psi  mathrmdA = int f^mathrmsrc  psi  mathrmdA\n\nfor all basis functions psi of the SE space. Taking psi = 1 makes this conservative. Expanding f^mathrmdst in the SE basis turns the condition into a linear system mathbfM  mathbff^mathrmdst = mathbfb with the mass matrix mathbfM_alphabeta = int psi_alpha psi_beta  mathrmdA. Both sides are block-diagonal over elements, so the problem reduces to a per-element solve. The local mass matrix is\n\nM^e_(ab)(cd) = int_e phi_a(xi)  phi_b(eta)  phi_c(xi)  phi_d(eta) \n                  J^e(xi eta)  mathrmdxi  mathrmdeta\n\n(assembled by compute_local_mass_matrix, with the index pairs flattened by ClimaCore.Utilities.linear_ind). Its integrand has degree 3(N_q - 1) per direction, so it is integrated with a GLL rule of order n ge (3N_q - 2)2; the Jacobian J^e is interpolated from its nodal values where needed (element_jacobian_at). The right-hand side reuses the intersection weights,\n\nb^e_ij = sum_k B(k(eij))  f^mathrmsrc_k \n\nand the per-element solve gives the assembled regridding operator,\n\nf^mathrmdste_ij = sum_ab big(M^e)^-1big_(ij)(ab) \n                          sum_k B(k(eab))  f^mathrmsrc_k \n\nUsing the full mass-matrix inverse — rather than dividing by the lumped diagonal W^e_ij — preserves constants exactly and is the optimal L^2 fit of the source on the SE basis over each element.\n\nTwo corrections appear in the implementation:\n\nQuadrature-consistency rescaling. The mass matrix is integrated by tensor-product GLL on the reference square, while B is integrated by triangulation of the physical polygon; the two cover slightly different domains. Before solving, each row of M^e is rescaled so its row sum matches the corresponding column sum of B, which restores exact constant preservation.\nEmpty-overlap fallback. A destination node may lie outside the source coverage (for example a tripolar ocean grid that stops well short of the South Pole regridded onto a full-sphere SE space), giving b^e_ij = 0. Only the covered nodes enter the local solve; uncovered nodes are left untouched rather than forced to zero.","title":"FV → SE"},{"category":"section","location":"extensions/climacore/#SE-SE","page":"ClimaCore Spectral Element Extension","text":"Direct SE to SE regridding is not supported: the Regridder constructor raises an error for two SE arguments and directs the caller to compose the two implemented directions, SE to FV to SE, with an intermediate finite-volume grid.","title":"SE → SE"},{"category":"section","location":"extensions/climacore/#Usage","page":"ClimaCore Spectral Element Extension","text":"","title":"Usage"},{"category":"section","location":"extensions/climacore/#Constructing-a-regridder","page":"ClimaCore Spectral Element Extension","text":"Regridder detects which argument is a ClimaCore.Spaces.AbstractSpectralElementSpace (or a ClimaCore.Fields.Field) and selects the corresponding direction. The first argument is the destination, the second the source.\n\nusing ConservativeRegridding, ClimaCore, Oceananigans\nusing ConservativeRegridding: regrid!\nusing ClimaCore: CommonSpaces\n\nspace = CommonSpaces.CubedSphereSpace(; radius = 6.371e6, n_quad_points = 4, h_elem = 16)\ngrid  = LatitudeLongitudeGrid(size = (360, 180, 1),\n                              longitude = (0, 360), latitude = (-90, 90), z = (0, 1))\n\nR_se2fv = Regridder(grid, space)   # SE source → FV destination\nR_fv2se = Regridder(space, grid)   # FV source → SE destination","title":"Constructing a regridder"},{"category":"section","location":"extensions/climacore/#Regridding","page":"ClimaCore Spectral Element Extension","text":"regrid! accepts and writes ClimaCore Fields directly on the SE side, flattening to and from the nodal layout internally (and applying a weighted direct stiffness summation when writing an SE destination):\n\nusing ClimaCore: Fields\n\n# SE → FV\nsrc_field = Fields.coordinate_field(space).lat\nfv_vals   = zeros(360 * 180)\nregrid!(fv_vals, R_se2fv, src_field)\n\n# FV → SE\ndst_field = Fields.zeros(space)\nregrid!(dst_field, R_fv2se, fv_vals)","title":"Regridding"},{"category":"section","location":"extensions/climacore/#Converting-between-ClimaCore-fields-and-flat-vectors","page":"ClimaCore Spectral Element Extension","text":"const ClimaCoreExt = Base.get_extension(ConservativeRegridding, :ConservativeRegriddingClimaCoreExt)\n\nsrc_vec = ClimaCoreExt.se_field_to_vec(field)        # Field → flat vector\nClimaCoreExt.vec_to_se_field!(dst_field, dst_vec)    # flat vector → Field\n\npositions = ClimaCoreExt.se_node_positions(space)    # Vector{UnitSphericalPoint}\nweights   = ClimaCoreExt.se_node_weights(space)      # nodal weights W^e_{ij}","title":"Converting between ClimaCore fields and flat vectors"},{"category":"section","location":"extensions/climacore/#Checking-conservation","page":"ClimaCore Spectral Element Extension","text":"On the SE side, ClimaCore.sum(field) computes the quadrature integral sum_eij W^e_ij f^e_ij and is the correct quantity for conservation checks. On the FV side the integral is the area-weighted sum:\n\nusing ConservativeRegridding: areas, Trees\nimport GeometryOps as GO\n\nfv_areas = areas(GO.Spherical(), Trees.treeify(grid))\n@assert sum(fv_vals .* fv_areas) ≈ sum(src_field)   # SE → FV conservation","title":"Checking conservation"},{"category":"section","location":"interfaces/trees/#Tree-Interface","page":"Tree Interface","text":"To use a grid with ConservativeRegridding.jl, implement two interfaces:\n\nGeometryOps SpatialTreeInterface - enables efficient spatial search via tree traversal\nConservativeRegridding.Trees interface - provides access to cell geometry","title":"Tree Interface"},{"category":"section","location":"interfaces/trees/#SpatialTreeInterface","page":"Tree Interface","text":"The regridder finds intersecting cell pairs using dual depth-first search on two spatial trees. Each node needs:\n\nBounding extent - a SphericalCap (or Extent for planar) that contains all descendant cells. Used to prune branches that can't intersect.\nChildren - either child nodes to descend into, or leaf status indicating this is a grid cell.\nLeaf identity - for leaf nodes, the index of the corresponding grid cell.\n\nimport GeometryOps: SpatialTreeInterface as STI\n\nSTI.isspatialtree(::Type{<:MyTree}) = true\nSTI.isleaf(node) = <true if this node represents a grid cell>\nSTI.nchild(node) = <number of children, 0 for leaves>\nSTI.getchild(node, i::Int) = <i-th child node (1-based)>\nSTI.node_extent(node) = <SphericalCap bounding all descendants>\n\n# For leaf nodes: return (cell_index, extent) pairs\nSTI.child_indices_extents(node) = ((cell_index, extent),)\n\nFor quadrilateral cells, use Trees.circle_from_four_corners(corners, ()) to compute the SphericalCap from 4 UnitSphericalPoints.","title":"SpatialTreeInterface"},{"category":"section","location":"interfaces/trees/#Trees-Interface","page":"Tree Interface","text":"The regridder needs to access cell geometry for intersection computation:\n\nusing ConservativeRegridding.Trees\n\nTrees.ncells(tree) = <total number of grid cells>\nTrees.getcell(tree, i::Int) = <polygon for cell i (1-based)>\nTrees.getcell(tree) = <iterator over all cell polygons>\nTrees.treeify(::GO.Spherical, tree) = tree  # passthrough if already a tree\n\nGOCore.best_manifold(tree) = GO.Spherical()  # or GO.Planar()\n\nCell polygons should use UnitSphericalPoint vertices for spherical grids:\n\nGI.Polygon(SA[GI.LinearRing(SA[p1, p2, p3, p4, p1])])","title":"Trees Interface"},{"category":"section","location":"interfaces/trees/#Hierarchical-Grids","page":"Tree Interface","text":"For grids with natural hierarchy (HEALPix, cubed sphere, etc.), the tree structure mirrors the grid's refinement hierarchy. You may need separate types for the root node (which may have different branching) and interior nodes.\n\nThe key insight: getchild encodes the parent-child relationship of your grid's hierarchy. The tree doesn't store the full structure—it computes children on demand from the node's position in the hierarchy.","title":"Hierarchical Grids"},{"category":"section","location":"interfaces/trees/#Checklist","page":"Tree Interface","text":"[ ] node_extent returns tight bounding SphericalCap\n[ ] isleaf correctly identifies leaf level\n[ ] child_indices_extents returns 1-based cell indices\n[ ] getcell returns polygon with correct vertex type\n[ ] best_manifold returns correct manifold type","title":"Checklist"},{"category":"section","location":"#ConservativeRegridding.jl","page":"Home","text":"ConservativeRegridding.jl provides functionality to regrid between two arbitrary grids. A grid is a tessellation of a space into polygons (or grid cells), each with an associated value. Data on a grid is referred to as a field, whereas the grid itself defines the tessellation.\n\nRegridding is performed conservatively, meaning the area-weighted mean is preserved. This is achieved by computing the intersection areas between all combinations of grid cells from the source and destination grids. These intersection areas provide the weights for averaging from neighboring cells.","title":"ConservativeRegridding.jl"},{"category":"section","location":"#Quick-Start:-SpeedyWeather-Grid-Transfer","page":"Home","text":"Here's an example of regridding between two different geodesic grids from SpeedyWeather.jl:\n\nusing SpeedyWeather\nusing ConservativeRegridding\n\nimport GeoInterface as GI\nimport GeometryOps as GO\n\n# Create random data on two different geodesic grid types\nfield1 = rand(OctaHEALPixGrid, 4)\nfield2 = rand(OctaminimalGaussianGrid, 4)\n\n# Get the polygon vertices for each grid (each column is a polygon as (lon, lat) tuples)\n# The Regridder constructor handles wrapping these as GeoInterface polygons and\n# fixing antimeridian crossings internally\nverts1 = RingGrids.get_gridcell_polygons(field1.grid)\nverts2 = RingGrids.get_gridcell_polygons(field2.grid)\n\n# Build the regridder (precomputes intersection areas)\nR = ConservativeRegridding.Regridder(verts1, verts2)\n\n# Regrid from field2 to field1\nConservativeRegridding.regrid!(field1, R, field2)\n\n# Regrid in the reverse direction using the transpose\nConservativeRegridding.regrid!(field2, transpose(R), field1)\n\nThe key advantage is that the Regridder only needs to be constructed once. After that, both forward regridding (via R) and backward regridding (via transpose(R)) can be performed efficiently without recomputing intersection areas.","title":"Quick Start: SpeedyWeather Grid Transfer"},{"category":"section","location":"#Mathematics-of-Regridding","page":"Home","text":"","title":"Mathematics of Regridding"},{"category":"section","location":"#The-Intersection-Area-Matrix","page":"Home","text":"Conservative regridding is built around a matrix A of intersection areas between source and destination grid cells. For a source grid with m cells and a destination grid with n cells, the matrix A is n times m, where each entry A_ij represents the area of intersection between destination cell i and source cell j.\n\nThe algorithm uses an efficient spatial tree structure (STRtree) to compute only the non-zero intersections, avoiding the O(nm) cost of checking all cell pairs.","title":"The Intersection Area Matrix"},{"category":"section","location":"#Forward-Regridding","page":"Home","text":"Let s be a vector of field values on the source grid and d the destination field values. The forward regrid operation computes:\n\nd_i = fracsum_j A_ij s_ja^d_i\n\nor in matrix form:\n\nd = fracA sa^d\n\nwhere a^d_i is the area of destination cell i, and the division is element-wise. The matrix-vector product As yields values weighted by intersection areas, and the division by a^d normalizes to obtain the regridded field values.","title":"Forward Regridding"},{"category":"section","location":"#Backward-Regridding","page":"Home","text":"The same intersection matrix A can be reused for backward regridding by transposing it:\n\ntildes = fracA^T da^s\n\nwhere a^s_j is the area of source cell j. The tilde on tildes emphasizes that the round-trip operation s to d to tildes does not recover the original field exactly—conservative regridding preserves the mean but generally reduces variance.","title":"Backward Regridding"},{"category":"section","location":"#Conservation-Property","page":"Home","text":"Conservative regridding preserves the area-weighted mean:\n\nfracsum_i d_i a^d_isum_i a^d_i = fracsum_j s_j a^s_jsum_j a^s_j\n\nThis property holds when both grids cover the same total area. The areas can be computed from the regridder via row and column sums of A:\n\na^d_i = sum_j A_ij quad textand quad a^s_j = sum_i A_ij","title":"Conservation Property"},{"category":"section","location":"#Implementation-Details","page":"Home","text":"In ConservativeRegridding.jl:\n\nSparse storage: The intersection matrix A is stored as a sparse matrix since most grid cell pairs do not intersect.\nSTRtree acceleration: A Sort-Tile-Recursive tree enables efficient spatial queries to find intersecting cell pairs.\nGeometryOps integration: Polygon intersections and area calculations are handled by GeometryOps.jl.\n~Antimeridian handling: Polygons crossing the antimeridian are automatically fixed via GeometryOps.fix.~\nNormalization: By default, the intersection areas are normalized to improve numerical conditioning.","title":"Implementation Details"},{"category":"section","location":"#API-Reference","page":"Home","text":"","title":"API Reference"},{"category":"section","location":"#Intersection-operator-interface","page":"Home","text":"These functions let you plug a custom weight kernel into the shared parallel assembly. See Customizing the weights: the intersection operator interface for a walkthrough.","title":"Intersection operator interface"},{"category":"function","location":"#ConservativeRegridding.intersection_areas","page":"Home","text":"intersection_areas(manifold, threaded, dst_tree, src_tree;\n                   intersection_operator = DefaultIntersectionOperator(manifold),\n                   npartitions = Threads.nthreads() * 4, progress = false)\n\nAssemble the sparse intersection matrix between src_tree and dst_tree on manifold. Lower-level assembly entry that intersection operators plug into; most users go through Regridder(…; intersection_operator = …).\n\nDriven by three dispatched seams on intersection_operator, each defaulting to the built-in area computation:\n\nIntersectionReturnStyle(op) — how each work item's contribution is stored.\nwork_items(op, candidate_pairs) — the units of work iterated.\noutput_matrix_size(op, src_tree, dst_tree) — the (nrows, ncols) shape.\n\nthreaded is a GeometryOpsCore.BoolsAsTypes (True()/False(); convert via booltype(::Bool)). When threaded, work items are partitioned into npartitions chunks assembled on separate tasks.\n\n\n\n\n\n","title":"ConservativeRegridding.intersection_areas"},{"category":"type","location":"#ConservativeRegridding.DefaultIntersectionOperator","page":"Home","text":"DefaultIntersectionOperator(manifold::GeometryOps.Manifold)\n\nDefault intersection operator for the given manifold.\n\nImplemented for Planar and Spherical manifolds at the moment. Will dispatch to the appropriate intersection operator / algorithm based on the manifold.\n\n\n\n\n\n","title":"ConservativeRegridding.DefaultIntersectionOperator"},{"category":"type","location":"#ConservativeRegridding.IntersectionReturnStyle","page":"Home","text":"abstract type IntersectionReturnStyle\n\nTrait for how an intersection operator delivers its contribution per work item during sparse-matrix assembly. Resolved once via IntersectionReturnStyle(op) at the top of intersection_areas and threaded through the parallel assembly, so the lookup never happens in the hot loop.\n\nSubtypes:\n\nOutOfPlaceSingleResult: kernel op(src_cell, dst_cell) -> area; driver stores the COO triplet.\nInPlace: kernel op(rows, cols, vals, item, src_tree, dst_tree) -> nothing pushes its own COO.\n\nDefaults to OutOfPlaceSingleResult, matching DefaultIntersectionOperator.\n\n\n\n\n\n","title":"ConservativeRegridding.IntersectionReturnStyle"},{"category":"type","location":"#ConservativeRegridding.OutOfPlaceSingleResult","page":"Home","text":"OutOfPlaceSingleResult <: IntersectionReturnStyle\n\nOne scalar per candidate pair. Kernel op(src_cell, dst_cell) -> area::Real; the driver stores the COO triplet (dst_index, src_index, area) when area > 0. Default style, used by DefaultIntersectionOperator.\n\n\n\n\n\n","title":"ConservativeRegridding.OutOfPlaceSingleResult"},{"category":"type","location":"#ConservativeRegridding.InPlace","page":"Home","text":"InPlace <: IntersectionReturnStyle\n\nA block of COO contributions per work item. Kernel op(rows, cols, vals, item, src_tree, dst_tree) -> nothing push!es onto rows/cols/vals itself. Used by block-emitting operators such as the ClimaCore spectral-element assemblers.\n\n\n\n\n\n","title":"ConservativeRegridding.InPlace"},{"category":"function","location":"#ConservativeRegridding.work_items","page":"Home","text":"work_items(op, candidate_pairs) -> items\n\nMap candidate (src_index, dst_index) pairs to the work units the parallel assembly iterates. Each item is destructured as (src_index, dst_index) for OutOfPlaceSingleResult or passed through verbatim for InPlace.\n\nDefaults to one unit per candidate pair. Override to change parallel granularity — e.g. to group all of an element's candidate cells into one unit.\n\n\n\n\n\n","title":"ConservativeRegridding.work_items"},{"category":"function","location":"#ConservativeRegridding.output_matrix_size","page":"Home","text":"output_matrix_size(op, src_tree, dst_tree) -> (nrows, ncols)\n\nShape of the sparse matrix intersection_areas assembles for op. Defaults to (prod(ncells(dst_tree)), prod(ncells(src_tree))) — dst cells as rows, src cells as columns. Operators whose counts differ from cell counts (e.g. spectral-element node counts) override this.\n\n\n\n\n\n","title":"ConservativeRegridding.output_matrix_size"},{"category":"section","location":"how_it_works/#How-it-works","page":"How it works","text":"This guide explains what happens under the hood when you create a Regridder and use it to regrid data. Understanding these internals can help you make better decisions about grid representations and debug unexpected behavior.","title":"How it works"},{"category":"section","location":"how_it_works/#The-high-level-picture","page":"How it works","text":"When you call Regridder(dst, src) on two grids dst and src, you enter the ConservativeRegridding pipeline.","title":"The high-level picture"},{"category":"section","location":"how_it_works/#1.-Input-sanitization-and-tree-construction","page":"How it works","text":"First, dst and src are converted into spatial tree structures via Trees.treeify.  This creates a data structure that:\n\nEfficiently describes the underlying grid structure,\nCaches expensive computations (like calculating the vertices of each cell, for e.g. tripolar grids),\nProvides a way to efficiently traverse the grid structure in a hierarchical way, from top level (very large) to leaf (each cell).\n\nThe object returned by treeify must implement the GeometryOps.SpatialTreeInterface as well as our internal Trees interface.   Several helper structs, like the quadtree representations and cursors, are made available to help with this, and those are used for e.g. the longitude-latitude, tripolar, cubed sphere, etc. grids. But an implementor can also create their own tree structures, as is done in the Healpix extension here.","title":"1. Input sanitization and tree construction"},{"category":"section","location":"how_it_works/#2.-Finding-potentially-intersecting-cells","page":"How it works","text":"Then, we descend simultaneously down into dst and src trees together, using a dual depth-first tree descent.   This is an algorithm in the class of dual-tree algorithms, which you can think of as aggressive pruning.   Both trees are walked simultaneously.  The children of each node are checked for intersection, and pruned if they don't intersect.\n\nAs an accelerator, we have an integrated multithreading step here.  Once we have descended low enough on the tree  for each node that it has enough children, we then spawn out a task for those nodes to continue the search on a different thread.   This allows us to multithread the search for intersecting cells, without having to manually partition the work.   No caching need be done ahead of time, since it's all baked into the algorithm.\n\nAt the end, we have a list of potentially intersecting cell pairs, represented as a vector of (dst_cell_index, src_cell_index) tuples.  Each index is an integer, starting from 1.  This index corresponds to the linear index of the data corresponding to that cell, in the  vector (flattened/vec) representation of the actual underlying dataset.","title":"2. Finding potentially intersecting cells"},{"category":"section","location":"how_it_works/#3.-Computing-regridding-weights","page":"How it works","text":"Now that we know which cells potentially intersect, we can compute the regridding weights.  These are stored in a sparse matrix,  where the row indicates the destination cell index and the column indicates the source cell index.  Each entry is the weight of  the value of the source cell that contributes to the destination cell.\n\nThere are many ways to procure this value.  For now, let's use the simplest method - first order, area based regridding - as an example. Here, we compute the area of intersection between the source and destination cells, which is then used directly as the weight.  Other methods, like second-order conservative regridding, bilinear, etc. will use different weight metrics, and some may not even use intersection areas at all.\n\nThe way this is done is by splitting up the list of candidates into chunks, and then computing the weights for each chunk in parallel.  We use  GeometryOps' optimized spherical convex polygon intersection algorithm (Sutherland-Hodgman) by default, but this can be overridden by the user.\n\nAt the end of this, each task returns a vector of (dst_cell_index, src_cell_index, weight) tuples.  These are then concatenated together, and  assembled into a sparse matrix which is the regridder object.\n\nSuccess!  We have now constructed a regridder.  There are a couple of steps after this, normalization and so on, but those are more details.","title":"3. Computing regridding weights"},{"category":"section","location":"how_it_works/#Customizing-the-weights:-the-intersection-operator-interface","page":"How it works","text":"Step 3 above is driven by a single extensible object — the intersection operator — plus three dispatched seams, each with a default that reproduces the area-based weight. The operator is passed as Regridder(dst, src; intersection_operator = ...), or directly to ConservativeRegridding.intersection_areas. The three seams are:\n\nIntersectionReturnStyle(op) — how the operator delivers a contribution for one work item:\nOutOfPlaceSingleResult (the default): the operator is a function op(src_cell, dst_cell) -> area::Real, and the driver stores the COO triplet.\nInPlace: the operator is op(rows, cols, vals, item, src_tree, dst_tree) -> nothing and pushes its own COO contributions directly.\nwork_items(op, candidate_pairs) — the units of work the parallel loop iterates over (default: one candidate (src, dst) pair per unit). Override it to change the parallel granularity — e.g. to group all of a source element's candidate cells into a single work unit.\noutput_matrix_size(op, src_tree, dst_tree) — the (nrows, ncols) shape of the assembled matrix (default: destination-cell count × source-cell count).\n\nEverything else — candidate search, chunking, multithreaded assembly, and sparse-matrix construction — is shared, so whatever your operator computes runs inside the same parallel skeleton as the built-in area weights.","title":"Customizing the weights: the intersection operator interface"},{"category":"section","location":"how_it_works/#The-simple-case-(OutOfPlaceSingleResult)","page":"How it works","text":"OutOfPlaceSingleResult is the default style, so a custom scalar-weight operator only needs to be callable; the defaults handle work units and matrix shape:\n\nusing ConservativeRegridding\nimport GeometryOps as GO\n\n# Reuse the built-in area weight, scaled by a constant factor.\nstruct ScaledAreaOperator\n    factor::Float64\nend\n(op::ScaledAreaOperator)(src_cell, dst_cell) =\n    op.factor * ConservativeRegridding.DefaultIntersectionOperator(GO.Spherical())(src_cell, dst_cell)\n\nregridder = ConservativeRegridding.Regridder(dst, src; intersection_operator = ScaledAreaOperator(2.0))","title":"The simple case (OutOfPlaceSingleResult)"},{"category":"section","location":"how_it_works/#Emitting-a-block-(InPlace)","page":"How it works","text":"When one work item should contribute several matrix entries — as the ClimaCore spectral-element assemblers do, emitting an Nq² block per intersection — declare the operator InPlace and push! directly onto the COO vectors:\n\nstruct MyBlockOperator\n    nrows::Int\n    ncols::Int\nend\nConservativeRegridding.IntersectionReturnStyle(::MyBlockOperator) = ConservativeRegridding.InPlace()\nConservativeRegridding.output_matrix_size(op::MyBlockOperator, ::Any, ::Any) = (op.nrows, op.ncols)\n\nfunction (op::MyBlockOperator)(rows, cols, vals, (i_src, i_dst), src_tree, dst_tree)\n    src_cell = ConservativeRegridding.Trees.getcell(src_tree, i_src)\n    dst_cell = ConservativeRegridding.Trees.getcell(dst_tree, i_dst)\n    weight = ConservativeRegridding.DefaultIntersectionOperator(GO.Spherical())(src_cell, dst_cell)\n    # A real block emitter pushes several (row, col, value) entries per work item;\n    # here we push a single one to illustrate the mechanics.\n    push!(rows, i_dst)\n    push!(cols, i_src)\n    push!(vals, weight)\n    return nothing\nend\n\nThe ClimaCore extension (SE → FV and FV → SE regridding) is built entirely on this interface: each direction is an InPlace operator, and FV → SE additionally overrides work_items to make the per-element mass-matrix solve the unit of parallel work.","title":"Emitting a block (InPlace)"},{"category":"section","location":"how_it_works/#How-we-actually-do-it","page":"How it works","text":"","title":"How we actually do it"},{"category":"section","location":"devnotes/#Developer-notes","page":"Developer notes","text":"This file contains some notes written during the development of this package, and is intended to help clarify why certain design decisions were made.","title":"Developer notes"},{"category":"section","location":"devnotes/#Why-the-regrid!-interface-is-complicated","page":"Developer notes","text":"Initially, we had one function called regrid!. That proved to be too much power in a single function, making dispatch complicated.\n\nTo solve this, we initially proposed to split the function into three: initialize_regridding!, perform_regridding!, and finalize_regridding!.\n\nI was wrong, when I thought that it was sufficient for initialize_regridding! to take only the source field and regridder, and finalize_regridding! to take only the destination field and regridder.\n\nThe case that breaks this is when you have one vector be a non-DenseArray and then the other vector be a DenseArray.  Consider the following:\n\nsrc::DenseVector, dst::NotDenseVector: you should do mul!(regridder.dst_temp, regridder.intersections, src) and then load regridder.dst_temp into dst.\nsrc::NotDenseVector, dst::DenseVector: you should load src into regridder.src_temp, then call mul!(dst, regridder, src_temp).  Then you are done.\nsrc::DenseVector, dst::DenseVector: there should be no loading of anything, just a straight mul!.\n\nEnabling the fast path for ::DenseVector, ::DenseVector then requires that you are aware of the natures of both src and dst.\n\nAnd this is not even getting started on what happens when you have multidimensional arrays.","title":"Why the regrid! interface is complicated"},{"category":"section","location":"devnotes/#The-new-API-that-solves-this","page":"Developer notes","text":"The solution to this is to have additional extractor functions that extract the vectors you want to do the regridding on.  Those can then be passed to regrid.  The new api surface is then:\n\nextract_source_vector(src, regridder) -> something\nextract_dest_vector(dst, regridder) -> something\n\nwhere something is anything that works with mul!.  Then you can do:\n\nfunction regrid!(dst, regridder, src)\n    src_arraylike = extract_source_vector(src, regridder)\n    dst_arraylike = extract_dest_vector(dst, regridder)\n    initialize_regridding!(regridder, src, src_arraylike)\n    perform_regridding!(dst_arraylike, regridder, src_arraylike)\n    finalize_regridding!(dst, regridder, dst_arraylike)\nend\n\nThis way, perform_regridding! can be assured what it's working on, and finalize_regridding! then has to know what the destination is. In this case I'm not sure what the use of initialize_regridding! is.  But it's probably fine to keep it.  Perhaps the split is that extract_source_vector only gets you the pointer in some sense, and initialize_regridding! actually loads data into it - if necessary.\n\nIn this case the dispatch limitations are, then, the following (where a type annotation ::T refers to something that you can dispatch on):\n\nextract_source_arraylike(src::T, regridder)::AbstractArray\nextract_dest_arraylike(dst::T, regridder)::AbstractArray\ninitialize_regridding!(regridder, src::T, src_arraylike) # - you should be assured what your src_arraylike is, but **must** always define `T`\nfinalize_regridding!(dst::T, regridder, dst_arraylike) # - you should be assured what your dst_arraylike is, but **must** always define `T`\nperform_regridding!(dst_arraylike::T1, regridder, src_arraylike::T2) # - first and third args must always be specified","title":"The new API that solves this"}]
}
